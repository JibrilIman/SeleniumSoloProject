package testFramework.pages;

import org.openqa.selenium.WebDriver;

public class ShoppingCartPage {

    private WebDriver webDriver;

    public ShoppingCartPage(WebDriver webDriver) {this.webDriver = webDriver;}
}
